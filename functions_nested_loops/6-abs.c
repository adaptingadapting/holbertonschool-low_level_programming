#include "main.h"

/**
 * _abs - computes the absolute value of an int
 * @i: int of which the abs value is calculated
 *
 * Return: abs value
 */

int _abs(int i)
{
if (i > 0)
return (i);
return (-i);
}
