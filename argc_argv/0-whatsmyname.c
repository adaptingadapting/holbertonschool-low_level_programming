#include <stdio.h>

/**
 * main - prints
 * @argc: num of args
 * @argv: array of strings
 * Return: nothing
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
